{
  "name": "amplitude-esbuild",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn run esbuild --bundle --outfile=build/build.js index.js"
  },
  "dependencies": {
    "@babel/runtime": "^7.16.3",
    "amplitude-js": "^8.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.13.14"
  }
}
